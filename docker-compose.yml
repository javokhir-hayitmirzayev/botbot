version: '3.8'

services:
  bot:
    build: .
    container_name: aiogram-bot
    env_file: .env
    environment:
      - DB_PATH=/app/bot.db
    volumes:
      - ./:/app
    restart: unless-stopped
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge
